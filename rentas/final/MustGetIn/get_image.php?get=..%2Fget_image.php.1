<?php

if(isset($_GET['get'])){
    $file = $_GET['get'];
    $image = "images/" . $file;
    if(file_exists($image)){
        $content = file_get_contents($image);

        header("Content-Type: image/png");
        print($content);
    }
}

?>
